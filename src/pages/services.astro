---
import BaseLayout from "../layouts/BaseLayout.astro";
import ServiceCard from "../components/ServiceCard.astro";
import CTABanner from "../components/CTABanner.astro";
import { services } from "../data/services";
---

<BaseLayout
  title="Services"
  description="Web development, mobile apps, custom automations, SEO, AEO, and more. TechHound delivers premium tech services at honest prices."
>
  <section class="px-4 py-10 md:py-14 sm:px-6 lg:px-8 bg-[#FAFAF8] dark:bg-[#111111]">
    <div class="container-default text-center">
      <h1 class="text-4xl sm:text-5xl font-heading font-bold mb-4">Our Services</h1>
      <p class="text-secondary text-lg max-w-2xl mx-auto">
        We build software and digital solutions for businesses of all sizes. Here's what we can do for you.
      </p>
    </div>
  </section>

  <!-- Services Grid -->
  <section class="section-padding">
    <div class="container-default">
      <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-8">
        {services.map((service) => (
          <ServiceCard
            title={service.title}
            description={service.description}
            icon={service.icon}
            items={service.items}
          />
        ))}
      </div>
    </div>
  </section>

  <CTABanner />
</BaseLayout>
